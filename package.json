{
  "name": "es-app-e2e",
  "private": true,
  "version": "0.1.0",
  "description": "Playwright system tests for LanLearn TOEFL MVP",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:install": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.0"
  }
}
